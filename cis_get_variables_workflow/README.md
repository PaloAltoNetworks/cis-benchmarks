# CIS Workflow

Supporting workflow to capture values from the device of interest and generate
input selection form data. These are used as inputs to captures and tests
that are specific to items like zones types (internal, internet, etc.).

